<h1>Listing pedidos</h1>

<table>
  <thead>
    <tr>
      <th>Fechapedido</th>
      <th>Fechaentrega</th>
      <th>Cliente</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pedidos.each do |pedido| %>
      <tr>
        <td><%= pedido.FechaPedido %></td>
        <td><%= pedido.FechaEntrega %></td>
        <td><%= pedido.Cliente %></td>
        <td><%= link_to 'Show', pedido %></td>
        <td><%= link_to 'Edit', edit_pedido_path(pedido) %></td>
        <td><%= link_to 'Destroy', pedido, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pedido', new_pedido_path, class: "btn btn-sucess"%>
<%= link_to 'Inicio', root_path, class: 'btn btn-warning'%>
